def get_context(question: str) -> str:
    """
    Stub function to retrieve context for a given question.
    Replace this with real implementation (e.g., vector database lookup).

    :param question: The user's current question
    :return: Context string to include in system prompt
    """
    return (
        "Faculty of Computer Science and Computer Engineering (FICT) at KPI is one of the leading faculties..."
    )
